{
  "description": "ai-brain-engine-v6 — injeção no início da sessão, prova do que chegou ao modelo e sensores por edição",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ["python", "${CLAUDE_PLUGIN_ROOT}/hooks/session_start.py"],
            "timeout": 10000,
            "statusMessage": "carregando o contexto do projeto"
          }
        ]
      }
    ],
    "InstructionsLoaded": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ["python", "${CLAUDE_PLUGIN_ROOT}/hooks/instructions_probe.py"],
            "timeout": 5000,
            "async": true
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": ["python", "${CLAUDE_PLUGIN_ROOT}/hooks/post_edit.py"],
            "timeout": 30000,
            "statusMessage": "medindo o arquivo editado"
          }
        ]
      }
    ]
  }
}
